services:
  postgres:
    container_name: spring-template-db
    image: postgres:16.0
    environment:
      POSTGRES_USER: root
      POSTGRES_PASS: root
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data